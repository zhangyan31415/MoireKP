# KP Canonical Smoke Fixture

This synthetic fixture exercises KP canonical layout without external
scientific data. Generate its ignored numeric inputs with:

```bash
python tests/fixtures/kp_canonical_smoke/generate_inputs.py
```

The Gamma and K1 configurations under `kp/configs/` use explicit toy bases and
are test fixtures, not release-facing material examples.
